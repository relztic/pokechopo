/**
 * @constant {string} mode The mode to apply.
 */
const mode = 'normal'

/**
 * @constant {Object} theme The theme of the application.
 */
const theme = Object.freeze({
  mode,
})

export default theme
