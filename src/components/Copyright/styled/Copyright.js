// External Packages
import styled from 'styled-components'

// Internal Packages
import { dimensions } from 'config/palette'

/**
 * @constant {Element} style The style of the component.
 */
const style = styled.span`
  bottom: 0;
  font-size: ${dimensions.xSmall};
  margin: ${dimensions.xxSmall};
  position: absolute;
  right: 0;
`

export default style
